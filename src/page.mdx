# Title

Hey, this is a MDX file. You can write Markdown in it. 

For example, here is so basic JSX code displayed:

```jsx
import React from "https://esm.sh/react"
```  
Now, we can also include React components in the MDX file. 

import { Warning } from './warning.js'

<Warning>
  <h3>Warning</h3>
  This is a warning message.
</Warning>  


